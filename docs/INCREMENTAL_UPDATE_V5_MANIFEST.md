# Incremental Update V5 Manifest | Manifiesto de actualización incremental V5

## Objetivo | Objective

Mejorar el README como guía rápida completa, manteniendo el detalle técnico en `docs/` y conservando la capa de wrappers por escenario. | Improve the README as a complete quick guide, keeping technical detail in `docs/` and preserving the scenario wrapper layer.

## Cambios principales | Main changes

| Archivo | Acción |
|---|---|
| `README.md` | Reorganizado con propósito, arquitectura, estructura, configuración, ejecución, validación, resultados, evidencias y documentación. |
| `README.en.md` | Versión equivalente en inglés. |
| `config/experiment.conf` | Mantiene parámetros sustentables paper-final y comentarios bilingües variable por variable. |
| `scripts/run/29–38` | Mantiene wrappers de ejecución D-E, validación, visualización, freeze y escenarios A-E. |
| `docs/17–21` | Documentación alineada con ejecución rápida, flujo no programador, estándar de mensajes, reproducibilidad y wrappers. |

## Compatibilidad | Compatibility

No se eliminan scripts `00–28`, no se eliminan resultados existentes, no se modifican freezes históricos y no se cambia la estructura base del laboratorio. | Scripts `00–28` are not removed, existing results are not deleted, historical freezes are not modified, and the base laboratory structure is not changed.
